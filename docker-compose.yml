version: '3.8'
services:
  hackathon-module:
    build: .
    ports:
      - "8010:8010"
    networks:
      - cpp-edge
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.hackathon-module.rule=PathPrefix(`/test/hackathon-module`)"
      - "traefik.http.services.hackathon-module.loadbalancer.server.port=8010"

networks:
  cpp-edge:
    external: true
