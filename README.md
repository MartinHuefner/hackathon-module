# Hackathon Module

Simple backend module that serves a single HTML page showing an image and a confirmation message.

To run locally:

```bash
npm install
npm start
```

To run with Docker Compose (gateway expects external `cpp-edge` network):

```bash
docker compose up -d --build
```
